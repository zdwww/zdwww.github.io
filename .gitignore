# Ignore the contents of the _site directory and other cache directories
_site/
.sass-cache/

# Ignore the directory for local files during development
local/

# Ignore the Gemfile that is generated
Gemfile.lock

# Ingore files created by npm
node_modules
package-lock.json

.DS_Store

# Local-only dev config (overrides site.url for `jekyll serve`)
_config.dev.yml

# Demo video — too large for repo; hosted on Google Drive
CS5610_P1_demo.mp4
demo.mp4
CS5610_P1_demo.srt

# Slides — Marp source + .pptx; deck lives on Google Slides
slides/
